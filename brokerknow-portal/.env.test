# Cedar TEST environment (cedartest.martensafrica.com). Cedar brand (defaults),
# same-origin /api, plus a TEST label that shows a warning ribbon + tab prefix.
# Build with:  npx vite build --mode test --base=/
VITE_API_URL=/api
VITE_ENV_LABEL=TEST
