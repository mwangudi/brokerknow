# PM Feedback - Testing Comments 2 (received 2026-06-02)

Source: `BK_Files/Docs/Testing Comments 2.docx`. Triaged into themes. Numbers refer to my backlog ID, not the doc order.

## A. Admin SPA — Header / Layout
- A1. Clock: prepend "MARKET " — `MARKET OPEN` (green) / `MARKET CLOSED` (red). [trivial]
- A2. Left menu: enable scrolling when expanded (can't reach lower items). [css fix]
- A3. Goal: dashboard should fit on one page without scrolling where possible. [layout]

## B. Dashboard
- B1. Replace "Top Brokers" tile with "Top Clients".
- B2. Replace "Pending Allocations" tile with "Most Active Stock".

## C. Auth / Users
- C1. Allow password reset + request a new one-time prompt (OTP).
- C2. (Portal) Admin portal user failed to load profile; the linked one worked. [bug — investigate]

## D. Reports
- D1. All Reports tab: remove the blue description (looks like demo text).
- D2. Client Listing download: include ALL fields (audit/inspection use).
- D3. Client Statement: search by Client Code (in addition to name).
- D4. Client Statement: date pickers must support calendar AND typing.
- D5. Contract Notes + Client Statements: use the same header layout/wording as legacy BK. Add "Generated By: <User>".
- D6. Statement: remove "Pending Settlements" section.
- D7. Statement: show Client Code, CSD Number, and Agent (if any) next to Client Name.
- D8. New: **Agents Report** — accumulated commission per agent (used for paying agents).

## E. Orders
- E1. Hardcode `Branch` or remove the field from the Order entry form.
- E2. Security Type label: use `Equity` / `Bond` instead of `F` / `S`.
- E3. Mode label: use `Priced` / `Market Price` instead of `P` / `BF`.
- E4. Replace validation word "Error" with friendlier wording (Alert, Action Needed, Attention, Notification…). [global UI string]
- E5. Settlement-date holiday calendar: T+3 already correct; let user input Holidays so we skip them. (Mirror legacy BK's holiday table.)
- E6. Block orders for: future dates, weekends, and defined holidays.

## F. Contract Notes
- F1. Remove the descriptions after Broker Commission (the "50k, etc.").
- F2. Use the same contractual wording as legacy BK.
- F3. PM will supply a signature image — paste on Contract Note; auto-stamp Date with current timestamp.
- F4. Allow delete/reverse a Contract Note (very-high-permission action). Confirm whether already implemented.

## G. Payments & Receipts
- G1. Maker/Checker workflow: one role creates, another approves; covers reverse/delete too.
- G2. Receipt: Bank field should list **internal ledger accounts only**.
- G3. Payment: when `EntityType = Client` → auto-pick "Client Payout"; `Agent` → "Agent Commission Payout"; etc.
- G4. **Bug**: payment allowed > client balance. Repro: client 448 (Dr Kennedy Malisita). Enforce balance check. [bug — investigate]

## H. Securities & Levies
- H1. Admin can add new securities (IPO pending).
- H2. Rename `CGT` → `WHT` (NB: also rename in legacy BK; calculation unchanged).

## I. Commissions
- I1. Allow creating new commission rates and editing existing ones (per-client override already exists — keep).

## J. Client Portal
- J1. Allow placing orders from the portal.
- J2. Onboarding greeting: "Welcome to Cedar Capital, let's get you started." + two choices:
  - "I am already a Cedar Capital Client"
  - "I am new, I would like to be a Cedar Client"
- J3. Registration: review-step before final submit (currently goes through on Next).
- J4. When linking a client, replace scroll with **search**.
- J5. Deposit screen: allow upload of proof of deposit.

---

## Suggested execution order
1. Quick UI wins (A1, A2, B1, B2, D1, D6, E1, E2, E3, E4, F1, H2) — visible to PM fast, low risk.
2. **Bugs** (C2 portal profile load, G4 over-payment) — fix before adding features.
3. Holiday + order-date guard (E5, E6) — domain change, needs a `Holiday` table + DateValidator.
4. Statement / Contract Note rework (D2-D7, F2, F3, D5, D8 Agents Report).
5. Payments workflow (G1 Maker/Checker, G2, G3) — material backend change.
6. Securities / Commissions admin (H1, I1).
7. Portal work (J1-J5) — JX is its own track, plan separately.
8. C1 password reset + OTP — touches auth pipeline; size before starting.

Items needing PM input:
- F3 signature image asset
- C1 OTP channel (email? SMS?)
- F4 confirm delete/reverse not already shipped
- G1 role list / permission matrix
